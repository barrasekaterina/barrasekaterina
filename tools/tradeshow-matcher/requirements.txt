Flask>=3.0
pandas>=2.0
recordlinkage>=0.16
openpyxl>=3.1

# Optional: only needed for the "pull live from CRM database" path.
# Also requires the system-level "ODBC Driver 17 for SQL Server" to be installed.
pyodbc>=5.0
